# Check for an interactive session
[ -z "$PS1" ] && return

HISTSIZE=4000

alias ls='ls -G'
alias la='ls -a -G'
PS1='[\u@\h \w]\$ '

alias units="units -v"

EDITOR="/usr/local/bin/vim"

# git related
alias gs="git status ."

